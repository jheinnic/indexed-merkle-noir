{
  "name": "indexed-merkle-noir",
  "version": "0.0.6",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "todo": "bash -c \"grep -rn 'TODO' --exclude-dir={node_modules,build,cache,lib,out,target} --exclude={*.min*,*.swp,package.json,solc} *\"",
    "test": "mocha -t 40000 --exit"
  },
  "author": "numtel <ben@latenightsketches.com>",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@zkpassport/poseidon2": "^0.6.2"
  },
  "devDependencies": {
    "@aztec/bb.js": "^2.1.9",
    "@noir-lang/noir_js": "^1.0.0-beta.17",
    "@noir-lang/noir_wasm": "^1.0.0-beta.17",
    "mocha": "^11.1.0"
  }
}
